import { Routes } from '@angular/router';
import { InCartComponent } from './pages/in-cart/in-cart.component';

export const cartRoutes: Routes = [
    {
        path: '',
        component: InCartComponent
    }
]